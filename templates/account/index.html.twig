{% extends 'base.html.twig' %}

{% block body %}

    <div class="example-wrapper">
        <h1>Espace personnel</h1>
       <a href="{{ path('app_logout') }}">Deconnexion</a>
       <a href="{{ path('app_logout') }}">Modifier votre mot de passe</a>

    </div>
{% endblock %}